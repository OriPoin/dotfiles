# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.

# C
O_FLAGS="-march=native -O3 -pipe"
GRAPHITE="-fgraphite-identity -floop-nest-optimize"
SSP_FLAGS="-fno-stack-protector"
IPAPTA="-fipa-pta"

# for valgrind 
STRLEN="-fno-builtin-strlen"

COMMON_FLAGS="${O_FLAGS} ${GRAPHITE} ${SSP_FLAGS} ${STRLEN} ${IPAPTA}"

CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
LDFLAGS="-Wl,-O3 -Wl,--as-needed -Wl,--hash-style=gnu -Wl,--sort-common"

# NOTE: This stage was built with the bindist Use flag enabled
PORTDIR="/var/db/repos/gentoo"
DISTDIR="/var/cache/distfiles"
PKGDIR="/var/cache/binpkgs"
PORTAGE_TMPDIR="/var/tmp/portage"

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C

# Makeopt
MAKEOPTS="-j9"

# Feature
FEATURES="splitdebug parallel-fetch -test"
AUTO_CLEAN="yes"

# Aria2
FETCHCOMMAND="/usr/bin/aria2c -d \${DISTDIR} -o \${FILE} --allow-overwrite=true --max-tries=32 --max-file-not-found=2 --max-concurrent-downloads=5 --connect-timeout=5  --timeout=5 --split=5 --min-split-size=2M --lowest-speed-limit=1K --max-connection-per-server=9 --uri-selector=feedback \${URI} --all-proxy="http://127.0.0.1:10809""
RESUMECOMMAND="${FETCHCOMMAND}"

# Accept
ACCEPT_KEYWORDS="amd64"
ACCEPT_LICENSE="*"

# Platform
LLVM_TARGETS="X86"
CHOST="x86_64-pc-linux-gnu"
CPU_FLAGS_X86="aes avx avx2 f16c fma3 mmx mmxext pclmul popcnt rdrand sse sse2 sse3 sse4_1 sse4_2 ssse3"
GRUB_PLATFORMS="efi-64"

# Hardware
VIDEO_CARDS="intel i965 iris nvidia"
ALSA_CARDS="hda_intel"
INPUT_DEVICES="libinput"

# L10N
L10N="en-US en"

# KVM

QEMU_SOFTMMU_TARGETS="x86_64"
QEMU_USER_TARGETS="x86_64"

# USE
HW="-bluetooth"
NET="wifi -idn"
DEV="git threads -vala jit lto -ssp pgo -fortran -gpm -initramfs jemalloc -man"
DE="imlib xinerama -postscript"
Audio="opus alsa pulseaudio"
Video="vulkan vkd3d vdpau vaapi opencl"
LANG="cjk -spell"
LOCALUSE="usbredir"
ZIP="zstd"
USE="${NET} ${DEV} ${DE} ${Audio} ${Video} ${LANG} ${HW} ${LOCALUSE} ${ZIP}"

GENTOO_MIRRORS="https://mirrors.tuna.tsinghua.edu.cn/gentoo"
